import 'package:flutter/widgets.dart';

class SuggestScreen extends StatelessWidget {
  static const String route = "SuggestScreen";

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("$route"),);
  }
}
