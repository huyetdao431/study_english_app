import 'package:flutter/material.dart';

class AppColors {
  // --- Primary Colors ---
  static const Color primaryBlue = Color(0xFF1E88E5);     // Màu chính
  static const Color primaryDark = Color(0xFF1565C0);     // Màu chính đậm
  static const Color primaryLight = Color(0xFF90CAF9);    // Màu nền nhẹ

  // --- Accent ---
  static const Color accentBlue = Color(0xFF42A5F5);       // Màu phụ trợ

  // --- Secondary / Neutral ---
  static const Color secondaryGray = Color(0xFF7199AD);    // Văn bản phụ, border

  // --- Backgrounds ---
  static const Color darkWhite = Color(0xFFF5F9FF);  // Nền chính
  static const Color white = Color(0xFFFFFFFF);   // Nền thẻ
  static const Color lightGray = Color(0xFFF1F1F1);       // Đường kẻ

  // --- Text ---
  static const Color black = Color(0xFF212121);       // Văn bản chính
  static const Color darkGray = Color(0xFF757575);     // Văn bản phụ

  // --- Status Colors ---
  static const Color successGreen = Color(0xFF43A047);      // Thành công
  static const Color warningOrange = Color(0xFFFB8C00);     // Cảnh báo
  static const Color errorRed = Color(0xFFE53935);          // Báo lỗi
  static const Color infoCyan = Color(0xFF00ACC1);          // Thông tin
}
