import 'package:flutter/widgets.dart';

class MiniGameScreen extends StatelessWidget {
  static const String route = "MiniGameScreen";

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("$route"),);
  }
}
